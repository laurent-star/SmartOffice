{
  "name": "smartoffice",
  "version": "1.0.0",
  "private": true,
  "description": "Outils internes Smart Office",
  "scripts": {
    "validate:n8n-ops": "node scripts/validate_n8n_official_ops.js",
    "generate:registries": "node scripts/generate_registries.js",
    "generate:workflows:tools": "node scripts/generate_tool_workflows.js",
    "validate:config": "node scripts/validate_config.js",
    "validate:workflows": "node scripts/validate_workflows.js",
    "validate": "npm run validate:n8n-ops && npm run validate:config && npm run validate:workflows"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
