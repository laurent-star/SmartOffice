{
  "name": "so.agent.planner",
  "nodes": [
    {
      "id": "node-trigger",
      "name": "Agent Trigger",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        200,
        300
      ]
    },
    {
      "id": "node-build-plan",
      "name": "Build Execution Envelope",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        420,
        300
      ],
      "parameters": {
        "jsCode": "const input = items[0].json || {}\nconst now = new Date().toISOString();\n\nconst baseMemory = input.payload?.memory ?? {\n  session_id: `sess_${Date.now()}`,\n  state: {},\n  stack: []\n};\n\nconst contextComplete = Boolean(baseMemory.state?.context_complete);\nconst steps = [];\n\nif (!contextComplete) {\n  steps.push({\n    type: 'capability',\n    ref: 'human_validation',\n    params: {\n      message: 'Compl\u00e9ter les informations manquantes avant planification.',\n      destination: 'agent',\n      reason: 'missing_context'\n    }\n  });\n} else {\n  steps.push({\n    type: 'tool',\n    ref: 'slack',\n    params: {\n      action: 'send_message',\n      channel: '#ops',\n      text: 'Plan valid\u00e9 et complet.'\n    }\n  });\n}\n\nconst header = input.header ?? {\n  id: `env-${Date.now()}`,\n  version: '1.0',\n  timestamp: now,\n  source: 'agent.planner',\n  destination: steps[0]?.params?.destination === 'agent' ? 'agent' : 'executor',\n  type: 'execution'\n};\n\nconst envelope = {\n  header,\n  payload: {\n    steps,\n    memory: baseMemory\n  }\n};\n\nreturn [{ json: envelope }];",
        "language": "JavaScript",
        "mode": "runOnceForAllItems"
      }
    }
  ],
  "connections": {
    "Agent Trigger": {
      "main": [
        [
          {
            "node": "Build Execution Envelope",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  }
}