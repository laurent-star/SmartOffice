{
  "name": "so.agent.supervisor",
  "nodes": [
    {
      "id": "node-trigger",
      "name": "Agent Trigger",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        200,
        300
      ]
    },
    {
      "id": "node-supervise",
      "name": "Build Execution Envelope",
      "type": "n8n-nodes-base.function",
      "typeVersion": 1,
      "position": [
        420,
        300
      ],
      "parameters": {
        "functionCode": "const input = items[0].json || {};\nconst now = new Date().toISOString();\n\nconst header = input.header ?? {\n  id: `env-${Date.now()}`,\n  version: '1.0',\n  timestamp: now,\n  source: 'agent.supervisor',\n  destination: input.payload?.steps?.length ? 'executor' : 'agent',\n  type: 'execution'\n};\n\nconst payload = input.payload ?? {};\nconst memory = payload.memory ?? {\n  session_id: `sess_${Date.now()}`,\n  state: {},\n  stack: []\n};\n\nconst steps = Array.isArray(payload.steps) ? [...payload.steps] : [];\nconst needsClarification = !memory.state?.context_complete;\n\nif (needsClarification) {\n  steps.unshift({\n    type: 'capability',\n    ref: 'human_validation',\n    params: {\n      message: 'Plan incomplet : collecter les informations manquantes.',\n      destination: 'agent',\n      reason: 'missing_context'\n    }\n  });\n}\n\nif (steps.length === 0) {\n  steps.push({\n    type: 'capability',\n    ref: 'human_validation',\n    params: {\n      message: 'Aucun step défini : demander les instructions manquantes.',\n      destination: 'agent',\n      reason: 'no_plan'\n    }\n  });\n}\n\nconst envelope = {\n  header: { ...header, destination: steps[0]?.params?.destination === 'agent' ? 'agent' : header.destination },\n  payload: { ...payload, steps, memory }\n};\n\nreturn [{ json: envelope }];"
      }
    }
  ],
  "connections": {
    "Agent Trigger": {
      "main": [
        [
          {
            "node": "Build Execution Envelope",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  }
}
