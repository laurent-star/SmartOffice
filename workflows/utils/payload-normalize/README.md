README — Utils / Payload Normalize

But

Nettoyer et normaliser un payload entrant (types, champs optionnels).

Utilisation

- Triggers : normaliser les evenements externes
- Agent : normaliser le contexte avant planification

I/O (attendu)

- Input : payload brut
- Output : payload normalise
