{
  "id": "cap.slack.notify.v1",
  "version": "1.0.0",
  "description": "Notify a Slack channel using a tool_call step.",
  "inputs": ["input.message", "context.slack.channel"],
  "outputs": ["memory.state.notification.text"],
  "steps": [
    {
      "type": "tool",
      "ref": "minimal-tool",
      "params": { "action": "do" }
    }
  ]
}
